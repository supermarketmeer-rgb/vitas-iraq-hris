# تطبيق الموظف الجديد - Employee App

## نظرة عامة
تم إنشاء تطبيق الموظف كمسار منفصل تماماً (Separate Route) للتأكد من عدم التأثير على أي من الموديولات الحالية في النظام.

**مهم**: تطبيق الموظف لديه نظام مصادقة منفصل يعتمد على رقم البادج وكلمة مرور افتراضية.

## المسار (URL)
- **رابط التطبيق الجديد**: `/employee-app/`
- **صفحة تسجيل الدخول**: `/employee-app/` (تلقائي عند عدم تسجيل الدخول)
- **لوحة التحكم**: `/employee-app/`
- **الرسائل**: `/employee-app/messages`
- **التنبيهات**: `/employee-app/notifications`
- **طلبات الإجازة**: `/employee-app/leave`
- **سجل الحضور**: `/employee-app/attendance`
- **الملف الشخصي**: `/employee-app/profile`

## المميزات

### 1. نظام تسجيل الدخول برقم البادج
- نظام مصادقة منفصل يعتمد على رقم البادج
- كلمة مرور افتراضية: `123456`
- يمكن للموظف تغيير كلمة المرور من الملف الشخصي
- يتم حفظ حالة تسجيل الدخول في `localStorage`

### 2. نظام الرسائل والتواصل
- إرسال واستقبال الرسائل
- عرض الرسائل مقروءة وغير مقروءة
- دعم الرد وإعادة التوجيه
- واجهة سهلة الاستخدام

### 3. نظام التنبيهات والإشعارات
- تصنيف التنبيهات (معلومات، تحذيرات، أخطاء، نجاح)
- فلترة التنبيهات (الكل، غير مقروء، هام)
- إمكانية تعيين التنبيهات كمقروءة
- تنبيهات هامة مميزة

### 4. نظام طلب الإجازات
- عرض رصيد الإجازات المتاح
- تقديم طلبات إجازة جديدة
- عرض سجل طلبات الإجازات السابقة
- تتبع حالة الطلبات (قيد الانتظار، مقبول، مرفوض)

### 5. سجل الحضور والبصمة
- عرض سجل الحضور اليومي
- إحصائيات شاملة (أيام الحضور، التأخير، الغياب)
- ملخص ساعات العمل
- عرض أحدث عمليات البصمة

### 6. الملف الشخصي
- عرض وتعديل المعلومات الشخصية
- عرض وتعديل المعلومات المهنية
- عرض وتعديل معلومات الاتصال
- إجراءات سريعة (تغيير كلمة المرور، التحقق من الهوية)

## الهيكل التقني

### الملفات المنشأة
```
src/employee-app/
├── EmployeeApp.tsx              # المكون الرئيسي للتطبيق (مع نظام مصادقة منفصل)
├── context/
│   └── EmployeeContext.tsx      # سياق الموظف المشترك
└── components/
    ├── EmployeeLogin.tsx        # صفحة تسجيل الدخول برقم البادج
    ├── EmployeeDashboard.tsx    # لوحة التحكم الرئيسية
    ├── EmployeeMessages.tsx     # نظام الرسائل
    ├── EmployeeNotifications.tsx # نظام التنبيهات
    ├── EmployeeLeaveRequests.tsx # طلبات الإجازة
    ├── EmployeeAttendance.tsx   # سجل الحضور
    └── EmployeeProfile.tsx      # الملف الشخصي (مع تغيير كلمة المرور)
```

### التكامل مع النظام الرئيسي
- تم إضافة المسار `/employee-app/*` في `src/App.tsx`
- يستخدم نظام مصادقة منفصل (EmployeeLogin)
- يستخدم `ErrorBoundary` منفصل لضمان الاستقرار
- لا يشارك السياق مع النظام الرئيسي
- يستخدم تصميم بصري متوافق مع النظام الرئيسي

## الفوائد

### 1. عدم التأثير على الموديولات الحالية
- مسار منفصل تماماً
- سياق منفصل (Context)
- لا يشارك الحالة مع التطبيق الرئيسي

### 2. سهولة الصيانة
- كود منظم ومفصول
- يمكن تعديل التطبيق دون التأثير على باقي النظام
- سهولة إضافة ميزات جديدة

### 3. تجربة مستخدم محسنة
- واجهة سهلة الاستخدام
- تصميم متجاوب (Responsive)
- دعم اللغة العربية

### 4. قابلية التوسع
- يمكن إضافة ميزات جديدة بسهولة
- هيكل مرن قابل للتخصيص
- دعم المستقبل للاتصال بقاعدة البيانات

## بيانات الاختبار

### مستخدمين تجريبيين لتطبيق الموظف
يمكن استخدام البيانات التالية لتجربة تطبيق الموظف:

#### موظف 1
- **رقم البادج**: `1001`
- **كلمة المرور**: `123456` (افتراضية)
- **الاسم**: أحمد محمد علي
- **القسم**: إدارة الموارد البشرية
- **المسمى**: مسؤول موارد بشرية

#### موظف 2
- **رقم البادج**: `1002`
- **كلمة المرور**: `123456` (افتراضية)
- **الاسم**: فاطمة حسين خليل
- **القسم**: المالية والمحاسبة
- **المسمى**: محاسب

#### موظف 3
- **رقم البادج**: `1003`
- **كلمة المرور**: `123456` (افتراضية)
- **الاسم**: علي كريم جاسم
- **القسم**: تقنية المعلومات
- **المسمى**: مسؤول تقني

## التطوير المستقبلي

### خطوات مقترحة للربط بقاعدة البيانات
1. إنشاء API endpoints للاتصال بقاعدة البيانات
2. استبدال البيانات التجريبية ببيانات حقيقية
3. إضافة مصادقة حقيقية (JWT/Cookies)
4. ربط نظام الإشعارات بقاعدة البيانات
5. ربط نظام الرسائل بقاعدة البيانات
6. ربط طلبات الإجازة بقاعدة البيانات
7. ربط سجل الحضور بنظام البصمة

### ميزات مستقبلية محتملة
- دعم إرفاق الملفات في الرسائل
- نظام تقييم الأداء الذاتي
- تقارير شاملة عن الحضور والإجازات
- دعم تعدد اللغات
- إشعارات Push للهاتف المحمول
- تكامل مع أنظمة البصمة الحقيقية

## ملاحظات مهمة

1. **المصادقة**: تطبيق الموظف يستخدم نظام مصادقة منفصل يعتمد على رقم البادج
2. **كلمة المرور الافتراضية**: `123456` لجميع الموظفين، يجب تغييرها فوراً
3. **الأمان**: كلمات المرور حالياً محفوظة محلياً للتجربة، يجب استخدام نظام تشفير آمن في الإنتاج
4. **البيانات**: جميع البيانات حالياً تجريبية، يجب ربطها بقاعدة البيانات
5. **التخزين**: يتم استخدام `localStorage` مؤقتاً للتنبيهات والرسائل وحالة تسجيل الدخول

## كيفية الاستخدام

### خطوات الوصول إلى تطبيق الموظف:
1. انتقل إلى `/employee-app/`
2. أدخل رقم البادج (مثال: `1001`)
3. أدخل كلمة المرور الافتراضية: `123456`
4. اضغط على "تسجيل الدخول"
5. ستظهر لك لوحة تحكم الموظف

### تغيير كلمة المرور:
1. بعد تسجيل الدخول، انتقل إلى "الملف الشخصي"
2. اختر تبويب "الأمان"
3. اضغط على "تغيير كلمة المرور"
4. أدخل كلمة المرور الحالية (123456)
5. أدخل كلمة المرور الجديدة
6. أكد كلمة المرور الجديدة
7. اضغط "حفظ التغييرات"

### ملاحظات مهمة:
- **كلمة المرور الافتراضية**: `123456` لجميع الموظفين
- **يجب تغيير كلمة المرور**: لأغراض الأمان، يُنصح بتغيير كلمة المرور بعد أول تسجيل دخول
- **تخزين كلمة المرور**: يتم حفظ كلمة المرور المشفرة محلياً للتجربة فقط

## الدعم الفني

لأي أسئلة أو مشاكل تقنية، يرجى الرجوع إلى:
- فريق التطوير
- التوثيق التقني للنظام
- مدير المشروع